# Field

**Proveniência:** código interno do Sistema de Romaneios neste workspace.

**Fonte:** `artifacts/romaneio/src/components/ui/field.tsx`

**Export público de origem:** `@/components/ui/field`

**Evidência de importância:** importado por 0 arquivo(s) consumidor(es) fora da biblioteca UI.

**Dependências de famílias:** `label`, `separator`.

**Lote:** 2. **Status:** pending.

A implementação, variantes, estados e exemplos completos serão relidos da fonte quando este lote for iniciado.

**Regra de fidelidade:** preservar API pública, comportamento Radix/shadcn, acessibilidade e estados; alterar apenas imports internos necessários para o pacote e conectar o estilo aos tokens Envia Daki.
